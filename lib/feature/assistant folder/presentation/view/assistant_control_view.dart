import 'package:flutter/material.dart';

class AssistantControlView extends StatelessWidget {
  const AssistantControlView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
